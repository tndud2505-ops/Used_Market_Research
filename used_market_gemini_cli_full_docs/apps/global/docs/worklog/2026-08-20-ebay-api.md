# 2026-08-20 eBay Browse API Integration

- Promoted eBay from an external search link to a United States internal source after Production credentials were issued.
- Added client-credentials OAuth with automatic Application access-token caching and renewal.
- Added official Browse API result normalization, pagination, safe eBay listing/image hosts, Worker allowlisting, and an internal eBay UI tab.
- Added a hidden local CMD credential flow that sends the Production Client ID and Client Secret through SSH stdin and verifies a real eBay result before keeping the change.
- Created and activated the Production keyset after marketplace account-deletion subscription verification.
- Added the public HTTPS callback `/global/api/ebay/account-deletion`, Cloudflare-secret challenge token, official SHA-256 challenge response, POST acknowledgement, and conservative D1 eBay-cache purge.
- Disabled D1 and runner result persistence for searches containing eBay and removed seller identifiers from normalized eBay items.
- Added DPAPI-protected browser-to-operator handoff and Windows release helpers so credentials never appear in command arguments or output.
- Verified the public production UI with 28 eBay results for `iphone 13`, working ascending and descending price sorts, official eBay links, and zero browser console warnings/errors.
- Credentials, generated access tokens, and private values are excluded from source, fixtures, logs, screenshots, and project documentation.
- Added the United States 4-source quality loop: deterministic failure fixtures first, then a 3-query × 4-source production matrix, then browser interaction verification.
- Fixed eBay `offset:N` cursor validation and enabled real 30-result load-more requests instead of a permanently disabled control.
- Added Vinted within-page windows and Vinted/Unclaimed next-page cursors so later batches do not overwrite or skip the current search session.
- Removed redundant login checks for all public sources, corrected relevance false negatives for short Vinted titles and Nike model names, excluded replacement screens from device searches, and kept quality counts internally consistent.
- Changed informational eBay sale-status warnings and ordinary relevance filtering from `Partial` to `Ready`; actual errors and high filter rates remain degraded states.
- The live matrix persists aggregate counts and rates only and never stores eBay listing rows or identifiers.
- Preserved exhausted-source state inside aggregate cursors so Poshmark and other completed sources are not recollected from page one while a paginated source continues.
- Added a three-hop bound for duplicate-only or filtered-empty intermediate batches instead of permanently hiding continuation after one zero-visible batch.
- Tightened category intent so `nike shoes` rejects apparel and `pokemon cards` rejects plush toys while retaining short marketplace titles and known shoe models.
- Extended definite-device-accessory filtering to nonnumeric models such as `iPhone SE` and `Pixel Fold`, with whole-device evidence preserving valid bundles.
- Split irrelevant-only browser results from selector drift and compute high-filter warnings from the total extracted window.
- Strengthened the production matrix with real second-window requests, cursor advancement, cross-page dedupe, and per-source usability gates.
- Final pre-release matrix: 12/12 searches passed; eBay 61, Poshmark 27, Vinted 66, and Unclaimed Baggage 41 visible listings; all validated price/link/image rates were 1.0; 9/9 supported cursor follow-ups advanced with zero cross-page duplicates.
- Browser QA found and promoted one additional real failure fixture: an eBay ear-speaker/proximity-sensor flex cable. Component signals now exclude it. Local low/high sorting uses numeric price first, matching the visible control label; quality warnings remain visible and break only equal-price ties.
- A Vinted-only browser probe then exposed selector evidence (`item`, `title`, `price`, `seller`) leaking into short-title relevance. Those structured keys are now stripped before category-intent checks, with extractor-to-normalizer coverage preserving valid `Nike` and `Nike Air` rows.
- Final production release: AWS runner `20260820T135419Z`, Cloudflare Worker `ee1af1e6-abac-48e3-94e5-5cd067b63e05`.
- Final production matrix: 12/12 passed with no warnings or failures; eBay 59, Poshmark 27, Vinted 68, and Unclaimed Baggage 38 visible listings; all validated price/link/image rates were 1.0; 9/9 supported cursor follow-ups advanced with zero cross-page duplicates.
- Final browser QA: eBay `iphone 13` expanded from 26 to 46 device listings with no component leak, local low-price order started at a complete $180 phone, the $200–$250 filter returned 13 in-range rows, Vinted returned 30 `nike shoes` rows with no apparel leak and 30 `pokemon cards` rows with no toy leak, and browser error/warning logs remained empty.
- Poshmark `ram` incident analysis showed the public collector was healthy (29 verified rows) while an origin-busy/release window was rewritten by the Worker and then reduced to a generic UI error. The Worker now preserves 429 and `Retry-After`, the UI keeps `SEARCH_UNAVAILABLE` distinct, and failed sources can be retried without discarding other marketplace rows.
- The `ram` relevance rule now requires computer-memory evidence such as DDR, DIMM/SODIMM, capacity, memory-module wording, or transfer speed. Rams apparel and zodiac items are excluded.
- Site tabs, sort, filters, and numbered pages now reuse an authoritative runner-owned memory session. Sessions expire after 10 idle minutes, use a 32-entry LRU, return 30 rows per page, cap verified rows at 1,000, and enforce each visible-window increase at no more than 160 rows within the 640 source-window budget. Search responses are not stored or replayed from D1.
- Post-release production matrix passed 12/12 searches with no warnings, failures, or unusable sources: eBay 57, Poshmark 27, Vinted 69, and Unclaimed Baggage 38 visible rows. All validated price/link/image rates were 1.0, and all 9 supported cursor follow-ups passed.
- Production Poshmark `ram` returned two verified memory listings across its first-window continuation, zero Rams apparel/zodiac leaks, and zero cross-page duplicates.
- Authoritative search-session release: AWS runner `20260820T150429Z`, Cloudflare Worker `d15ab275-1460-4657-b43c-3b089588d016`.
- Post-session production matrix passed 12/12 with no warnings, failures, or unusable sources: eBay 55, Poshmark 27, Vinted 67, and Unclaimed Baggage 38 visible rows. All validated price/link/image rates were 1.0; all nine supported cursor follow-ups passed; the aggregate site view reused the same stable generation without collector work.
- A live sparse-result probe found that a server-returned `session.window=1` could not be sent back because validation required at least 30. The validator now accepts 1–1,000 while the session store still enforces the authoritative +160 progression. The regression is fixed in `harness/server-search-session-contract.mjs`.
- Final Poshmark `ram` probe returned one current Corsair memory listing in 2.7 seconds, source state `Ready`, zero normalized source errors, and zero Rams apparel/zodiac leaks. A same-session price view returned HTTP 200 without another collection.
