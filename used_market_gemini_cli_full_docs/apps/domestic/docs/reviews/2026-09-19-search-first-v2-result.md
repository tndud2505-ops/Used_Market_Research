# Search-first v2 구현·검증 기록

기준: `docs/design/2026-09-19-search-first-v2/PROMPT.md`와 같은 폴더의 01–03 이미지. 이미지의 가상 가격·날짜·표본, 지역/상태 필터, eBay 원화 환산, `ONE 이용 안내`는 구현 대상에서 제외했다. 기존 `apps/domestic`를 수정했으며 새 앱이나 새 가격 계산기를 만들지 않았다.

## 요구사항별 구현

| 요구 | 적용 내용 |
| --- | --- |
| 검색 상단 정리 | 홍보 제목·영문 슬로건·설명·서비스 배지 제거. 접근성용 h1은 유지하고 카테고리 탭을 흰색 공통 헤더 바로 아래 배치. |
| 조밀한 검색 도구 | 모델 선택·거래 사이트·검색창을 하나의 도구 영역으로 통합. 기존 카테고리별 필터·정렬·가격 범위·페이지 이동 유지. |
| 한 모델도 매물 목록 | 모델 선택은 검색 범위만 변경. 모델 수와 매물 수를 분리하고 하나의 모델에 속한 여러 매물, 0건, 1건 모두 동일한 목록 구조로 표시. |
| 가격 그래프 분리 | 검색의 분석 패널·가격 요약·차트 및 통계 요청 제거. 결과 도구 줄에 단일 `가격 그래프 보기 ↗` 링크. `target=_blank`, `noopener noreferrer`와 모델·출처·안전한 원래 검색 주소 전달. |
| 가격 분석 전체 폭 | 제목/현재 매물 링크 한 줄, 모델 선택 dialog, 기존 단일 출처·기간 선택, 전체 폭 차트와 출처별 native table. |
| 설명 중복 제거 | 비교 기준/매물 조건 카드와 별도 안내 행동 제거. 출처·통화·실제 기간·가격·각 표본은 표에, 비교 단위와 판매완료 표시가의 의미는 caption에 표시. |
| 빌더 통합 표 | 9종 부품을 부품/모델/수량/판매중 가격/판매완료 표시가/변경의 6열로 구성. 표의 tfoot에 판매중·판매완료 합계 및 각각의 가격 확인 수량 통합. |
| 기존 행동 유지 | 부품 찾기·명시 저장·자동 저장·수량 입력/증감·모델 변경·제거·인쇄·호환성 정보·키보드 조작 유지. |
| 가격 안전장치 | 기존 core/data 모듈 및 API의 publication/window/currency 검증·대표가격 기준·소수 단가 계산·부분 합계·저장 실패 처리 유지. 미확인 가격은 0원이 아님. |
| 모바일 | 1440/1024/768/390/360px 검수. 모바일 검색은 기존 native 필터 dialog를 이용하고 빌더·출처 표는 동일 정보를 읽을 수 있게 행 내부를 재배치. |

## 구현 중 재현하여 수정한 문제

- 이전 스타일의 grid-area/order가 새로운 검색 도구를 잘못 배치하고 매물 목록의 폭을 제한하던 충돌을 해소했다.
- 이전 빌더 스타일의 선택자가 변경·제거 버튼을 세로로 늘리고 모바일 수량을 덮던 문제를 수정했다. 브라우저 검증에 실제 hit-test와 데스크톱 행 높이 검사를 추가했다.
- 페이지 이동의 다음 프레임에서 검색창으로 옮긴 입력 초점을 빼앗던 동작을 수정했다. 사이트 선택과 필터 재렌더링 시에도 키보드 초점을 유지한다.
- native search 입력이 Esc를 소비하더라도 모델 dialog는 한 번에 닫히고 원래 행동으로 초점이 돌아오게 했다. IME 조합 중 취소는 별도로 존중한다.
- 가격 조회 재시도는 즉시 로딩 상태를 다시 표시한다. 잘못된 기간·통화 응답이나 늦은 이전 요청으로 현재 가격이 오염되지 않도록 기존 경계를 유지한다.

## 배포 전 확인

- `npm test`: 전체 결정론 테스트 통과. 기존 데이터/분류/수집 정책/게시/가격 안전성 테스트를 유지하고 폐기한 화면 배치에 대한 UI 계약만 새 요구사항으로 갱신했다.
- 저장소 루트 `scripts/verify.ps1`: 전체 결정론 테스트 재실행 통과. `git diff --check`도 통과했다.
- `node cloudflare/deploy.mjs --preflight-only`: AWS 수집·게시 준비 상태 통과.
- `harness/pc-search-first-browser.mjs`: loopback에서 통제된 합성 응답을 사용하는 13개 브라우저 검증 묶음 통과. 실제 브라우저에서 여러 모델/한 모델/매물 0·1건, 새 탭, 키보드 사이트 전환, 기간 오류, 통화·게시기간 불일치, 503 재시도, 부분 합계, 서로 다른 publication 거부, 인쇄 lifecycle, 저장 거부를 확인했다. 합성 503 한 건은 의도한 오류 검사이며 운영 장애가 아니다.
- 같은 하네스를 실제 공개 API에 연결한 후보 화면에서도 통과했다. 운영 데이터를 변경하거나 매물·가격 API를 모의 응답으로 대체하지 않았다. 9종/10개 선택의 실제 API 단가와 화면 합계를 별도로 대조했고, 미확인 가격은 판매중/판매완료 각각 부분 합계로 제외됨을 확인했다.
- 후보 실데이터 검수의 page error 및 예상하지 않은 1차 API 오류는 0건이었다. 데스크톱·모바일·인쇄 미디어 스크린샷을 직접 확인했다.

로컬 실행 결과·캡처는 Git 제외 경로 `tmp/search-first-v2/`에 보관한다. 합성 결과(`fixture-report.json`)와 실제 공개 API 결과(`candidate-live-report.json`)를 구별한다. 가격·날짜는 검수 시점의 관측이며 이후에도 같은 수치를 보장하는 문서가 아니다.

## 배포 범위와 상태

UI/정적 자산 변경이므로 기존 `cloudflare:app-release` 경로를 사용한다. D1 migration, AWS Runner 교체, 원장/통계 재게시, 수집·분류 로직 변경은 이번 범위에 포함하지 않는다. 기존 release 스크립트의 하네스·dry-run·이전 Worker 버전 확보·두 도메인 smoke 및 실패 시 정확 버전 rollback을 유지한다.

### 실제 배포 및 운영 재검증 완료

- 배포 소스 커밋: `3e4a5e81541ce6ad137e3ba831a6c8133fc48c77` (`feat: implement search-first v2 listing and PC tools UI`).
- 명령: `npm run cloudflare:app-release`, 종료 코드 0, 최종 `status: released`.
- 활성 Worker 버전: `e9ff46f1-e61f-413f-a850-7234e62a7a84`. 배포 후 `wrangler deployments status`에서 해당 버전이 트래픽 100%를 담당하는 것을 재확인했다.
- `https://used-pick.com`: health, HTML, 보안 헤더, 카테고리 API 검사 통과. 실제 운영 브라우저의 검색/페이지 이동/별도 그래프/원래 검색 조건 유지/사이트 키보드 선택/eBay USD 분리/모델 선택/가격 표/구성 저장·수량·제거/모바일 검수 8개 묶음 통과.
- `https://www.used-pick.com`: 기존 canonical HTTPS 주 도메인으로의 301 이동을 유지한다. 경로·검색 조건 보존을 확인하고 주 도메인에서 같은 운영 브라우저 흐름까지 확인하여 9개 묶음 통과했다. 두 개의 별도 앱이 아니라 같은 앱의 별칭이다.
- 각 주소로 요청한 변경 자산과 핵심 가격 모듈 12개씩, 총 24개 응답의 SHA-256이 검수한 로컬 파일과 일치했다. www에서는 검증된 동일 경로의 주 도메인 이동만 허용했다. 검사에 cache-bypass용 query를 추가하지 않았다.
- 운영 두 경로의 page error, 콘솔 error, 1차 정적 자산 오류, 1차 API 오류는 모두 0건이었다. 운영값으로 합성 수치나 예시 가격을 삽입하지 않았다.
- 배포 후에도 1440/1024/768/390/360px의 페이지 전체 가로 넘침과 수량 조작부 가림을 검사했다. 실제 운영 390px 캡처에서 부분 합계, RAM 수량, 가격 미확인 표시, 변경·제거·호환성 정보가 읽히는 것을 직접 확인했다.

증거: `tmp/search-first-production-release.log`, `tmp/search-first-worker-status.json`, `tmp/search-first-v2/release-asset-audit.json`, `tmp/search-first-v2/used-pick-com-report.json`, `tmp/search-first-v2/www-used-pick-com-report.json`. 운영 보고서와 캡처는 생성 결과이므로 커밋하지 않는다. 배포 후 문서 및 www 검증 하네스의 변경은 서비스 자산을 바꾸지 않는다.

## 재현 방법

`pc-search-first-browser.mjs`는 기존 Playwright browser 인스턴스를 받아 독립 context에서 검수하고 종료한다. 실제 운영은 `fixture:false`, 합성 오류 검사는 loopback origin에서만 `fixture:true`를 허용한다. 실행자는 먼저 저장소의 `external-ai-orchestrator/SKILL.md` 및 브라우저 지침을 확인해야 한다. 테스트는 별도 실행이며 일반 `npm test`에 운영 접속을 추가하지 않는다.

외부 AI 계정/보조 에이전트는 사용하지 않았다. 로스터 실행 파일이 없었으므로 기본 작업자가 직접 구현하고, 설치된 Node/npm/npx와 Playwright 경로로 브라우저를 검수했다. 로그인·프록시·MCP·승인 설정은 변경하지 않았다.

## 중단 후 재개 검수

- 이전 완료 기록만으로 판단하지 않고 프롬프트·참고 이미지 3장·구현 소스·미커밋 변경·실제 배포 상태를 다시 대조했다. 이미 배포된 서비스 자산은 변경하지 않았고 불필요한 재배포나 데이터 재게시를 하지 않았다.
- 루트 `scripts/verify.ps1`를 다시 실행해 내부 `npm test` 전체가 통과했으며, 실행 결과는 `tmp/search-first-resume-verify.log`의 `Deterministic suite passed`와 종료 코드 0으로 확인했다.
- 합성 경계조건 13개 묶음과 운영 주 도메인 8개 묶음의 새 브라우저 보고서가 통과했다. 순차 파이프 실행 자체는 두 보고서 출력 후 종료 코드 1로 끝났으므로 이를 전체 실행 성공으로 기록하지 않았다. 빠진 www 경로는 별도 일회성 Playwright 실행으로 다시 검증하여 9개 묶음 통과와 종료 코드 0을 확인했다.
- 두 운영 경로의 페이지·콘솔·자체 API·정적 자산 오류는 0건이었다. 390px 검색·가격 분석·구성표 캡처와 데스크톱 캡처를 직접 재검토했다. 합성 검사에서 의도적으로 발생시킨 503은 운영 오류와 구분한다.
- 활성 Worker 버전과 두 주소의 12개 파일씩을 다시 조회해 위 배포 버전의 트래픽 100%, 정상 health, 로컬 파일과의 SHA-256 일치를 확인했다. 재개 검수에서도 가격 계산·수집·인증 설정과 다른 작업의 파일은 변경하지 않았다.
