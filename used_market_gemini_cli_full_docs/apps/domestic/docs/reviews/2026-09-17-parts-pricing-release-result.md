# USED PICK 부품·가격 작업 실행 결과 — 2026-09-17

## 최종 결론: 운영 배포·게시·브라우저 검증 완료

아래 차단 기록 이후 작업을 재개해 원인을 Cloudflare Worker CPU 초과로 확정했다. SSH·토큰·D1 용량 문제는 아니었다. 31.1MB 전체 게시를 최대 40행 단위의 비활성 staging으로 바꾸고, 62개 조각의 checksum·순서·행 수·범위를 모두 검증한 뒤 한 번만 활성화하도록 수정했다.

최종 운영 게시 `1d112ab0-6f45-4224-8ba3-a5d026427d86`가 2,465행으로 활성화됐다. 첫 성공 게시 `b4e10072-2262-4e68-939c-5c2d1e0e57ac` 이후 행 계약과 D1 재조회 checksum 검증을 강화해 같은 데이터로 다시 게시한 결과다. normalization 18 / `pc-parser-v8` / `pc-rules-v18` / `pc-filter-v7`, target set `pc-targets:5:full-master-v13` 2,441개가 운영 중이며 정식 배포 준비 검사도 통과한다. 최종 Worker는 `9ec9ef1d-0beb-465c-b03f-f79e9c02ce3d`다.

전체 공개 감사에서 798개 도구 노드가 모두 HTTP 200이었고, 가격 대상 788개는 모두 v18이었다. G.Skill·gskill·G SKILL·G-SKILL·지스킬은 동일한 27개 ID를 반환했다. 실제 브라우저에서는 35개 모델, 9개 부품군·RAM 2개 수량의 독립 합계, 차트·검색·URL 복원·모바일 표를 확인했고 브라우저 오류와 실패한 자체 API는 0이었다. 최종 `scripts/verify.ps1`도 통과했다.

D1 active pointer와 공개 가격 API가 모두 최종 게시 ID를 반환한다. Runner·Tunnel을 재시작한 뒤 로컬/공개 health와 `node cloudflare/deploy.mjs --preflight-only`도 다시 통과했다.

운영 복구 DB는 `/var/lib/used-market-runner/backups/parts-v18-20260917T021100Z/search-index.sqlite`, SHA-256은 `b8d20efb18d4e24dff475bc692dee8b81b48392dea894ba80d53e216c147ba98`이다. 미등록·제외 80개는 여전히 추가 검토 후보이며, eBay 871개 대상은 이번 제한 표본 수집에서 실행하지 않았다.

## 이전 차단 시점 기록

기존 변경을 인수해 실제 소스 수정, 데이터 검증 재사용, 최신 DB 복사본의 변경분 처리, 실제 RAM 제목에 대한 추가 결함 수정, 전체 하네스, 공개 API 전수 감사를 수행했다. 그러나 **운영 서비스를 정지하고 최신 원본 DB의 복구 사본을 만드는 실행 요청이 연결 도구의 보안 판정 단계에서 차단**됐다. 해당 명령은 실행되지 않았고, 운영 DB·분류·게시·Runner·Worker/UI는 이전 상태다.

오류 원문:

> 요청의 보안 상태를 결정하지 못해 이 도구 요청은 OpenAI에 의해 차단되었습니다.

SSH 읽기, 서버의 격리된 스테이징 처리, 기존 D1 조회·통계 백업은 성공했다. 따라서 위 오류를 SSH 키 만료, 서버 sudo 권한 거부, Cloudflare 인증 실패, 수집 사이트의 403/429로 해석하지 않는다. 다른 경로나 표현으로 운영 제어 차단을 우회하지 않았다. Worker의 로컬 dry-run 요청도 같은 도구 오류로 거부되어 성공으로 기록하지 않았다.

필요한 외부 조치는 **이미 승인된 운영 백업·서비스 제어·정식 배포 요청을 실행할 수 있는 Chat On Steroids 연결 세션의 확보**다. 이 응답만으로 특정 UI 설정이나 재연결이 반드시 문제를 해결한다고 확인할 수는 없다. 서버 키·토큰을 채팅에 보내거나 재발급할 근거는 없다.

## 1. 소스와 실제 운영 상태

실제 작업 루트는 `C:\DeVelop\USED_WEB`, 앱은 `used_market_gemini_cli_full_docs/apps/domestic`이다. 최초 HEAD는 `612a60fc1e742c9d01182f415becaeda3eebb902`였으며, 기존 수정 파일을 삭제·초기화하지 않았다.

| 항목 | 실제 확인 결과 |
|---|---|
| 공개 기준 확인 | 2026-09-17 12:02:56 KST, 일반 사용자 URL |
| 공개 전체 감사 종료 | 2026-09-17 12:06:42 KST |
| 운영 카탈로그 | `public-pc-4`; 공개 729개, 도구 노드 795개 |
| 운영 가격 선택 대상 | 785개 + 가격 선택 불가 메인보드 탐색 노드 10개 |
| 운영 가격 정규화 | v17 / `pc-parser-v7` / `pc-rules-v17` / `pc-filter-v6` |
| 운영 수집 대상 | `pc-targets:4:full-master-v12` |
| 준비한 수정본 | 카탈로그 v5, 가격 선택 788개 + 탐색 노드 10개, 정규화 v18 |
| 준비한 버전 묶음 | `pc-parser-v8 / pc-rules-v18 / pc-filter-v7 / pc-master-v5` |
| 준비한 수집 대상 | `pc-targets:5:full-master-v13`, 대상 2,441개 |
| 실제 새 배포 | 없음 |
| 배포와 수정 소스 일치 | 불일치: 운영은 기존 소스, 수정본은 로컬 및 격리된 R3 소스에 존재 |

현재 운영 Worker는 `cc892532-f8de-4810-89ce-20ffe6ed9f92`가 100%인 상태를 복구 지점으로 확보했다. 이번 작업에서 이 버전을 변경하지 않았다.

정식 준비 상태 검사 `node cloudflare/deploy.mjs --preflight-only`는 exit 2였다. 실제 사유는 새 대상 집합 불일치, 중고나라·번개장터·eBay별 대상 수 불일치, 최근 게시 부재다. `--app-only`는 사용하지 않았다.

## 2. 지스킬 문제: 운영 전후를 혼동하지 않음

### 현재 일반 공개 검색

| 검색어 | 등록 항목 수 |
|---|---:|
| G.Skill | 27 |
| gskill | 0 |
| G SKILL | 0 |
| G-SKILL | 27 |
| 지스킬 | 0 |

모두 HTTP 200이었지만 등록 집합이 다르므로 운영 검색은 FAIL이다. 수정본에서는 다섯 표기의 ID 집합을 비교하는 회귀 검사를 추가했고 통과했다. **운영 배포 후 동일 집합이 됐다는 결과는 아직 없다.**

현재 G.Skill 27개 가격 API를 모두 확인했다. 관측 표본이 있는 항목은 12개, 양쪽 표본이 없는 항목은 15개였다. 12개 중 판매중 또는 판매완료 표본이 1건뿐인 항목 2개는 대표가격을 만들지 않았다. 모든 응답에 정확한 새 게시 ID가 없었다. 소스별 내부 모순은 `ddr3:4gb`, `ddr5:16gb`, `ddr5:64gb`에서 확인됐다. 개별 결과는 `tmp/pc-resume-full-audit-before.json`의 G.Skill 27행에 남겼다.

일반 API 원응답 예: `ram:g-skill:ddr4:16gb`는 판매중 11건/판매완료 5건인데 `mean`과 `median`이 null이고 `average`만 124,090.91/116,000으로 제공되며, 구성원 수는 null이었다. 이 이전 버전 요약을 새 버전의 정확한 기간 통계나 독립 검증된 견적 가격이라고 보고하지 않는다.

### 실제 관측에서 추가로 고친 RAM 단가 결함

`32GB (16GB×2)`처럼 합계 용량과 개당 용량·수량이 명확한데도 `KIT`라는 단어가 없으면 가격 범위를 모호하다고 제외하던 결함을 찾았다. 이제 일관된 전체 용량이 명시된 경우 총액으로 해석하고, `개당 / each / per module`은 명시적 단가로 우선 처리한다. 전체 용량과 모듈 수가 충돌하거나 수량만 있는 불명확한 매물은 계속 제외한다.

실제 저장 관측 `bunjang:431848897`, “지스킬 ddr4 4000 32gb (16기가x2) 램 팝니다.”는 격리된 수정 결과에서 다음과 같이 확인됐다.

| 값 | 검증 결과 |
|---|---|
| 등록 항목 | `ram:g-skill:ddr4:16gb` |
| 전체 용량 / 모듈 용량 | 32GB / 16GB |
| 수량 | 2 |
| 원매물 총액 | 290,000 KRW |
| 모듈 단가 | 145,000 KRW |
| 계산 | 145,000 × 2 = 290,000 |
| 시장·상태 | 국내 개인 간 중고 / 정상품 |

이것은 저장된 실제 매물의 단가 검증이지 새로 수집한 매물이나 해당 모델의 대표 시세가 아니다. 검사한 정상 두 모듈 G.Skill 예제 30개에서 단가×수량=총액이 맞았다. USD 관측은 해외 시장으로 분리된 상태로 검사했다.

## 3. 재사용한 데이터와 추가 검증

기존 봉인 결과의 181,194개 관측을 현재 운영과 대조했다. 스냅샷은 같아도 일부 원본의 마지막 확인 시각·조회 횟수가 바뀌어 엄격한 전체 일치 검사를 그대로 통과시킬 수 없었다. 검사를 낮추는 대신, **모든 원본 열과 스냅샷 열이 완전히 같은 관측만 재사용**하고 나머지만 현재 자료로 다시 분류하도록 수정했다.

현재 사본 199,225건에서는 136,125건을 재사용했다. 변경된 원본에 연결된 45,069건과 이후 신규 18,031건, 합계 63,100건만 추가 처리했다. 실제 RAM 결함을 발견한 후에는 23,694개 RAM 결과만 갱신했고, 나머지 175,531개 비RAM 결과는 같은 해시로 보존했다. 원본·관측 데이터는 덮어쓰지 않았다.

최종 분류 스테이징은 12:00:33 KST에 통과했다. 품질 고정 사례 127건의 분류·모델·RAM 수량 정확도는 1, 묶음 오염·잘못된 중복 제거·잘못된 판매완료·시장 혼합 차단 지표는 0이었다. **이 수치는 127개 회귀 사례의 결과이지 시장 전체 관측을 사람이 검토한 정확도가 아니다.** 이전 84개 가격 그룹 검증은 이번 추가 RAM 수정 이전 결과이므로 새 전체 가격 게시의 통과 근거로 재사용하지 않았다.

### 9개 부품군의 실제 확인 범위

아래 왼쪽 열은 운영의 기존 API, 오른쪽 열은 수정된 격리 사본의 최신 고유 매물 상태다. 서로 다른 범위·시점이므로 동일 수치나 운영 후 가격으로 비교하지 않는다. 신규 대상 실수집·운영 저장·운영 가격 반영은 **모든 행에서 BLOCKED / 미실행**이다.

| 부품 | 운영 등록 노드 | 기존 API 표본 있는 노드 | 기존 API 모순 노드 | 사본 최신 매물 | 사본 비교 가능 매물 |
|---|---:|---:|---:|---:|---:|
| CPU | 186 | 151 | 36 | 13,731 | 5,371 |
| GPU | 94 | 89 | 29 | 16,233 | 8,735 |
| RAM | 216 | 69 | 13 | 10,994 | 2,583 |
| 메인보드 | 39¹ | 20 | 6 | 2,413 | 145 |
| SSD | 70 | 48 | 15 | 9,453 | 4,490 |
| HDD | 40 | 36 | 17 | 4,353 | 1,427 |
| 파워 | 84 | 58 | 20 | 3,965 | 1,664 |
| 케이스 | 36 | 0 | 0 | 771 | 232 |
| 쿨러 | 30 | 0 | 0 | 781 | 202 |

¹ 기존 메인보드 노드 39개에는 가격 선택 불가 탐색 노드 10개가 포함된다. 새 수정본은 총 798개 도구 노드 중 가격 대상 788개와 해당 탐색 노드 10개를 따로 검사한다. 비교 가능한 개별 매물이 있다는 사실은 특정 모델의 30일 표본이 3건 이상이라는 뜻이 아니다. 케이스·쿨러도 원자료가 있지만 기존 공개 API 표본이 0인 상태이므로 단순히 “시장의 데이터가 없다”고 처리할 수 없다.

### 실제 미분류·미등록 검사와 남은 지원 한계

최신 관측 중 UNKNOWN 5,915개를 별도로 집계했고, 등록되지 않은 실제 매물 80개를 제목·분류·제외 사유로 점검했다. 원자료는 보호된 `pc-v18-corrected-market-evidence.json`에 있으며, 로컬에는 `tmp/pc-release-sealed-stage-summary.json`으로 읽기 전용 증거를 가져왔다.

휴대전화 케이스·포토카드·악기 케이스 등 “PC/케이스” 검색 잡음, 고장난 노트북 로직보드, 완제품·묶음은 부품별 정상 중고 시세와 분리해야 한다. 반면 Intel 랜카드를 CPU/메인보드로 분류한 경우, `WD SN520 NVME SSD 128G` 같은 미등록 표현, 리안리 케이스의 한글/모델 표현 등은 **추가 정밀 검토 후보**다. 모두 정상 제외라고 단정하지 않았다. 현재 해당 예시는 등록 가격 경로에서 제외되어 있지만 분류·검색 지원 범위의 한계가 남아 있다. 이 80개 표본은 최근 관측에 편향돼 있으며 전체 시장을 대표하거나 모든 미등록 부품을 검증한 결과가 아니다.

## 4. 게시·수집·견적·화면

정기 게시 수정본은 관측 범위와 이전 게시의 **범위 ID만** 합쳐 모든 통계를 같은 버전·시각으로 재계산한다. 이전 가격 값은 복사하지 않으며 부분 제품 게시를 일반 정기 게시 경로에서 금지한다. 반환된 게시 ID/checksum/행 수/범위 수/보존 행 0/병합 false가 모두 맞아야 성공이다. 통계 구성원 수와 checksum을 저장·노출하고, 게시와 수집의 쓰기를 직렬화했다. 게시 직후의 정리 작업이 30일 통계 구성원 근거를 지우지 않도록 관측 상세 보존 범위도 맞췄다. 관련 회귀 검사는 통과했지만 실제 정기 실행 성공은 아직 없다.

실수집 도구는 승인된 중고나라·번개장터에 정확한 검색어로 새 요청을 한 번만 보내고, 캐시·별칭 검색 결과를 새 수집 증거로 대체하지 않는다. 403/429/CAPTCHA 또는 잘못된 응답은 중단·실패·미실행으로 구분한다. 준비된 검증 대상은 G.Skill 한글 27개, 다른 가격 부품군 대표 8개, 넓은 카테고리 검색 9개로 소스당 44개다. **이번 릴리스의 실제 실행 건수는 0이다.**

전체 목표 2,441개의 소스별 배정은 번개장터 1,570, 중고나라 1,570, eBay 871개다. 격리 사본에서 모두 미실행이며, 활성 설정을 성공 시각으로 만들지 않았다. 운영의 이전 수집 대상이 계속 수집했다는 사실은 이 신규 대상의 실행 증거가 아니다.

9개 부품군·RAM 2개, 총 10개 수량의 독립 API 합산 검사와 UI 비교는 `tmp/pc-live-v18-browser.mjs`에 구현돼 있다. API 값만으로 별도 합산하며 앱 집계 함수로 기대값을 만들지 않는다. **배포 후 통과 결과는 없다.** 기존 운영을 별도로 진단한 파일은 `tmp/pc-release-current-browser-baseline/report.json`이며 새 버전의 성공 증거로 쓰지 않는다. 새 릴리스가 설치되지 않았으므로 `pc-resume-full-audit.mjs --after`와 v18 브라우저 합격 판정을 실행 완료로 표시하지 않는다.

D1 매물 대체 경로는 현재 설정상 비활성화돼 있으며 이번 작업에서 이를 변경하지 않았다. 기존 `republish-pc-projections.mjs`가 일반 공개 API 결과를 D1 결과로 간주하는 부분은 주의가 필요하다. 실제 Worker는 일반 매물 API를 Runner로 보내므로 그 조회만으로 D1 일치를 증명할 수 없다. 향후 실제 D1 행을 직접 검증·백업한 뒤 별도 정합성을 확인해야 한다. 이 경로를 이번에 갱신했다고 보고하지 않는다.

### 기존 운영의 실제 9종·10개 수량 화면 진단

12:10 KST의 실제 브라우저에서 CPU/GPU/RAM/메인보드/SSD/HDD/파워/케이스/쿨러 9종과 RAM 수량 2, 총 10개 수량을 불러왔다. 기존 화면은 판매중 **1,250,012원**, 판매완료 **1,141,204원**을 각각 **가격 확인 7/10개**로 표시했다. 의도적으로 다른 CPU·보드 소켓과 DDR 규격을 선택했으며 실제로 `LGA1700 / AM5`, `DDR5 / DDR4` 충돌 경고가 표시됐다. 이는 추천 조합이 아니다.

그러나 같은 시점의 원 API에는 필요한 정확한 평균/중앙값과 새 게시 ID가 없어, 새 릴리스의 독립 검증기는 합계를 확정하지 못했다. 기존 `average`나 최솟값으로 빠진 값을 채우지 않았다. 따라서 위 화면 금액을 **검증 완료된 정확한 견적**이라고 보고하지 않는다. 원 API 9개와 단가 누락 사유 11개, 화면 텍스트, 수량 목록, 실제 스크린샷이 별도 기준 보고서에 저장돼 있다. 첫 분석 페이지 진단은 숨겨진 카테고리 선택기를 기다리는 테스트 오류가 있어, 공개 검색 입력을 기다리도록 진단 도구를 수정했다. 이를 서비스 오류나 새 릴리스 통과로 처리하지 않는다.

## 5. 테스트·증거·복구 지점

| 검증 | 실제 결과 | 증거 |
|---|---|---|
| 최종 R3 앱 `npm test` | PASS | `tmp/pc-release-r3-npm-test.log` |
| 최종 루트 `scripts/verify.ps1` | PASS | `tmp/pc-release-r3-root-verify.log` |
| 전체/추가 계약 및 diff 검사 | PASS | 위 로그, `git diff --check` |
| 최신 관측 재사용·변경분 분류 | PASS, 격리 사본만 | `tmp/pc-release-current-rehearsal.log` |
| 실제 RAM 결함 보완 | PASS, RAM만 갱신 | `tmp/pc-release-ram-corrected-stage.log` |
| 실제 저장 키트 단가 | PASS, 정상 예제 30개 | `tmp/pc-release-corrected-market-evidence.log` |
| 현재 공개 API 전체 조회 | 실행, baseline | `tmp/pc-resume-full-audit-before.json` |
| 정식 배포 준비 상태 | FAIL, exit 2 | `tmp/pc-release-formal-preflight-blocked.log` |
| 최신 원본 DB 백업·서비스 중단 | BLOCKED, 명령 미실행 | 연결 도구 오류 원문 |
| Worker dry-run | BLOCKED, 명령 미실행 | 연결 도구 오류 원문 |
| 실제 신규 수집·운영 전체 게시 | BLOCKED | 실행 증거 없음 |
| 배포 후 전체 API·브라우저 | BLOCKED | 배포 자체가 없음 |

복구 지점:

* 이전 Worker: `cc892532-f8de-4810-89ce-20ffe6ed9f92`, 100%; `tmp/pc-release-worker-before.json`.
* 이전 D1 활성 게시: `e0175ff5-cb88-42b1-a2a2-789ba3287c8d`, 2,294행, checksum `7d3c3723451d271da095ec3a3d57e2f995d1e875ef6eba3efd2272d14a67a3c4`. D1은 v16, 기존 Runner는 v17인 불일치가 이미 존재했다.
* 전체 가격 게시 테이블 SQL 백업: `tmp/pc-release-d1-backup-20260917.sql`, 240,162,487바이트, SHA-256 `da3947ecb156612c2b3771ebdd6481ba9a3a4e5bf5e064007c7f9b0ae541cd9f`. Git에 넣지 않는다.
* 서버 코드·서비스·설정: `/var/lib/used-market-runner/backups/parts-v18-20260917T021100Z/runtime-and-settings.tar.gz`, SHA-256 `0de6e11461be70864bd5931a7928445baa5108f2ff49e5a0b6abdf801e00a60e`. 보호된 파일이며 비밀값을 출력·커밋하지 않았다.
* 수정본 패키지: `/tmp/used-pick-parts-v18-20260917-r3.tar.gz`, 574,314바이트, SHA-256 `4496ed8f1f58af1d54cecdc96a07e95d558949e691a3586e474b5392cab7a350`.
* 최종 분류 사본: `/var/lib/used-market-runner/staging/parts-v18-20260917/pc-v18-current.sqlite`, 검증 보고서 `pc-v18-corrected-ram-report.json`. **원본 운영 복구 파일로 사용하면 안 된다.**

최신 원본 DB의 stopped-service 복구 사본은 차단으로 만들지 못했다. 따라서 “완전한 복구 준비가 끝났다”고 할 수 없다. 운영 변경이 없으므로 이번 실행의 롤백은 필요하지 않았다. 나중에 수집 후 장애가 생겼다면 오래된 DB로 덮어써 새 관측을 잃는 복구를 사용하면 안 된다.

## 6. 완료 판정과 커밋

인수인계서의 모든 완료 기준은 `2026-09-17-completion-checklist.md`의 PASS/FAIL/BLOCKED 표에 기록했다. 코드·테스트의 통과와 운영 완료를 구분했다. 임시 도구·JSON·SQL·백업·브라우저 프로필은 커밋 대상에서 제외하고 관련 소스·테스트·검수 문서만 선별한다. 최종 소스 커밋 ID와 종료 시점의 서비스/작업 잠금 확인은 후속 실행 기록 절에 명시한다.

운영 배포, 새 한글 대상의 실제 저장·게시, 정확한 새 견적과 화면의 일치가 확인되지 않았으므로 **작업 완료가 아니다.**

## 종료 시점 실행 기록

- **소스·테스트·검수 문서 커밋:** `8fcabf5f5f9132bae8dbeee7a6fe61968f2ea8f7` — `fix: validate parts pricing cohorts, RAM units and staged catch-up`. 53개 파일을 선별했다. 수정본 패키지의 SHA-256은 위 R3 값과 같고, 스테이징에 봉인한 중요 소스·테스트 8개 해시도 커밋한 작업 트리와 모두 일치했다.
- 데이터·SQL·브라우저 프로필·비밀값은 커밋하지 않았다. 원래 루트에 있던 관련 없는 임시 배포 스크립트 3개도 그대로 남겼다. 원격 push나 운영 배포를 수행했다는 뜻이 아니다.
- **최종 서버 확인:** 2026-09-17 **12:18:23 KST**. Runner HTTP 200, ledger 준비됨, 정기 수집 활성, Runner·터널 서비스 모두 active. 수집 대상은 여전히 v12이고 `publication_recent=false`, 마지막 게시 성공은 `2026-09-14T13:00:29.118Z`였다. 증거는 `tmp/pc-release-closure-state.json`이다.
- 단일 실행 소유 잠금을 정상 종료했고 서버에서 `RELEASE_OWNER_REMOVED`를 확인했다. 중단된 장기 배포 프로세스를 남겨 두지 않았다.
- 서버에서 신규 복구 사본이 없음을 다시 확인했다(`PRODUCTION_BACKUP_COMMAND_NOT_EXECUTED`). 차단된 서비스 정지·백업 명령은 실행되지 않았으며 기존 운영 서비스는 유지됐다.
- 기존 화면 브라우저 진단의 첫 시도 보고서는 별도 폴더에 보존했다. 검색 입력을 기다리도록 수정한 재시도에서도 분석 페이지 입력 노출 대기가 실패해 분석 화면을 확인 완료로 기록하지 않았다. 오류 원인이 현재 페이지와 진단 도구 중 어디에 있는지까지는 확정하지 않았다.
- 컴퓨터 맞추기의 실제 모바일 스크린샷을 육안으로 확인했다. 문서 전체의 수평 넘침이 없어도 모델명·가격 열이 잘리는 부분이 있으므로 기존 모바일 화면을 가독성 PASS로 처리하지 않았다. 새 CSS의 운영 검증도 미완료다.
