# 에이전트 2 후속 실행 결과 — 2026-09-17

## 판정

**로컬 UI 수정·결정론 검사 완료. 운영 자산 일치·실제 브라우저 재검증은 BLOCKED / 미실행. 배포 완료 보고가 아니다.**

사용자의 “1번 다 했어, 너도 이어서 해줘” 요청으로 14:57 KST에 재개했다. HEAD는 `b993ab70070909911eb1c96876d4c6e6afb548f0`였다. 최초 6개 UI 자산은 이전 v3 해시와 같았고, 앞서 전달한 단가 표시·조건 초기화 패치는 아직 공유 소스에 적용되지 않았다. 이번에는 해당 수정을 실제 앱 소스에 적용하고 추가 통계 준비 상태 결함도 수정했다.

`UI_SOURCE_EDITING_STOPPED`: 이 역할의 추가 소스 편집을 종료한다. 아래는 인수용 후보이며 운영 배포 승인이 아니다. 운영 DB·수집·게시·서비스·Worker/D1 쓰기·git add/commit/push·package.json 변경은 이 실행에서 수행하지 않았다.

## 1. 이번에 실제로 적용한 변경

| 항목 | 수정 전 문제 | 이번 수정 |
|---|---|---|
| 단가와 수량 표시 | 통계 단가 126,237.5를 126,238로 표시하면서 2개 금액은 252,475로 표시 | `126,237.5원 × 2 = 252,475원`으로 단가·행 금액 정밀도를 맞춤. 원 API와 합계 계산값은 변경하지 않음 |
| 더 긴 소수 통계 | 표시 반올림으로 양변이 정확히 같지 않을 수 있음 | 예: 원 단가 100.335이면 `100.34원 × 2 ≈ 200.67원`. 원 단위 최종 합계 반올림 안내 추가 |
| 조건 초기화 | 저장 부품의 카테고리를 다시 선택하면서 제조사·세대를 재적용 | 필터·검색어·정렬만 초기화. 저장 견적, 수량, 사이트 및 기간은 유지 |
| 통계 미제공 상태 | 화면은 소문자 `unavailable`만 확인; 새 서버 계약은 `UNAVAILABLE` | 대소문자 상태를 통일해 확인. `NO_EXACT_PUBLICATION`도 시장 표본 0건으로 취급하지 않음 |
| 미제공 값의 유입 | 준비 중 응답의 진단용 대표값이 합계·정렬·차트에 남을 수 있음 | 준비되지 않은 자료를 견적·가격 정렬·차트에서 배제하고 미제공 사유 표시 |
| 과거 조회 | 응답의 요청 기간과 실제 게시 요약기간이 달라도 ready 가능 | `published_window`와 요청 응답 `window`가 다르면 오류로 처리. 현재 가격을 과거 기간으로 대체하지 않음 |
| 과거 기간 오류 안내 | 새 `HISTORICAL_EXACT_STATS_UNAVAILABLE` 코드가 일반 실패로만 표시 | 기존 역사 통계 오류 코드와 함께 선택 기간 통계 미제공 안내에 연결. 임의 원 응답 본문은 노출하지 않음 |
| 모바일 카테고리 띠 | 음수 오른쪽 여백으로 내용 영역 밖으로 확장 | 음수 여백 제거·최대 폭 제한. 띠 내부 스크롤은 유지하고 본문 넘침을 숨기는 방식은 사용하지 않음 |

모바일 변경은 **CSS 및 소스 회귀 검사 통과**다. 실제 390×844 렌더링의 1px 넘침이 없어졌다는 판정은 브라우저 재실행 전까지 보류한다.

기존 메인 검색 7종 / 가격 도구 9종 / 내부 수집 11종 범위를 바꾸지 않았다. 제품 master, 분류 의미, 가격 정책, 수집 소스, 운영 통계를 변경하지 않았다. 프런트엔드 자산 후보는 **`parts-ux-v4`**이며, 정규화 버전이나 카탈로그 버전을 올렸다는 뜻이 아니다.

기존 `02-agent2-precision-reset.patch`는 이번 통합 소스에 의해 **대체됐다**. 이전 v3용 패치를 현재 파일에 다시 적용하지 않는다.

## 2. 실행한 테스트와 결과

| 검사 | 결과 | 실제 증거 |
|---|---|---|
| v3에서 최초 7개 결함 재현 | 7개 FAIL | `tmp/pc-agent2-resume-baseline-20260917.log` |
| 최종 후속 회귀 검사 | **8/8 PASS**, 실패·생략 0 | `tmp/pc-agent2-resume-final-functions-20260917.log` |
| 기존 역할 2 UI 계약 | **PASS**, 가격 선택 788 / 탐색 전용 10 별도 검사 | `tmp/pc-agent2-resume-final-ui-20260917.log` |
| 앱 전체 `npm test` | **PASS / exit 0** | `tmp/pc-agent2-resume-npm-test-20260917.log` |
| 마지막 표시 보완 후 루트 `scripts/verify.ps1` | **PASS / exit 0** | `tmp/pc-agent2-resume-root-verify-20260917.log`; 실행 세션 40625 종료 코드 확인 |
| 브라우저·공개 감사 스크립트 문법 | **PASS / exit 0** | 실제 `node --check` 실행. 스크립트를 실제 실행했다는 뜻은 아님 |
| 범위 제한 `git diff --check` | **PASS / exit 0** | 중간 검증 실행 결과. 마지막 재검사 묶음은 도구에서 거부돼 별도 미실행 |
| 15:10:20 후보 해시 저장 | **완료**, 10개 파일 | `tmp/pc-agent2-resume-candidate-hashes-20260917.json` |
| 루트 검사 후 해시 재대조 | **BLOCKED / 미실행** | 실행 전 연결 도구 거부. 15:10 해시를 검사 후 재확인했다고 주장하지 않음 |

초기 수정 후 6개가 통과했고 reset 검사에서는 테스트용 환경이 실제 `action` 생성 함수를 빠뜨려 실패했다. 해당 화살표 함수와 `el` 생성 함수를 실제 소스에서 가져와 실행하도록 테스트 환경을 보완했다. reset 검사를 제거하거나 실제 필터 초기화 함수를 통째로 가짜 구현으로 바꾸지 않았다. 이후 7개가 통과했고, 높은 정밀도 통계의 근사 기호 검사를 추가해 최종 8개가 통과했다.

새 검사는 소스 함수 실행·가벼운 테스트 노드·합성 HTTP 응답에 기반한다. 실제 브라우저 이벤트·레이아웃·외부 서비스 성공을 대신하지 않는다. 788개는 등록된 항목의 필터 경로 검사이지 실제 시장 매물 전수 검사나 788개 가격 확보가 아니다.

등록 검사 수: GPU 94, CPU 186, RAM 216, 메인보드 32, SSD 70, HDD 40, PSU 84, 쿨러 30, 케이스 36. 탐색 전용 메인보드 10개는 가격 선택에서 배제한다.

## 3. 브라우저 진단 도구도 수정했으나 실행하지 않음

대상: `tmp/pc-live-v18-browser.mjs`. 기존 파일은 먼저 `tmp/pc-live-v18-browser-before-agent2-resume-20260917.mjs`로 보존했다. 보존 전후 SHA-256은 `28d0189e81bf3b8d3b0d498aaff0fe722287ae6a3ffc9e57e85cf40890f36b64`로 같았다.

이번 도구 개선:

- 앱 `coherentStats`, `metricValue`, `money` 의존을 제거하고 원 API로 독립 기대값을 계산한다.
- 실제 필수 스킬을 읽고 적용한 운영자가 `--run --skill-file <실제 경로>`를 지정해야 한다. 스킬 파일 입력 해시는 기록하지만, 파일 존재 자체를 규칙 준수의 증명으로 삼지 않는다.
- 브라우저 생성 전에 일반 HTML URL과 v4 자산의 공개·로컬 바이트 해시가 같은지 검사한다. 구버전 화면을 새 후보의 합격 증거로 사용하지 않는다.
- `?qa=full` 캐시 회피 주소를 제거했다. 9종 견적은 원래 URL과 견적 hash로 연다.
- `판매중 N건` 문구만 기다리지 않고 모델 ID·`aria-busy`·가격 상태로 대기를 끝낸 뒤 ready인지 명시적으로 검사한다. 오류/미제공을 성공으로 바꾸지 않는다.
- 단가 식, reset, 9종·10개 수량, 동일 게시·기간·버전·시장·통화, 행별 합계 정보를 추가 검사한다.
- 모바일 폭의 +1px 허용을 제거했다. 단순 폭 검사 외 화면 육안 검토는 여전히 필요하다.
- 고정 3초 대기 대신 실제 9개 수량 입력 렌더링을 기다린다. 실행별 증거 폴더를 분리하며 조기 실패도 보고서를 남기고 만든 브라우저만 종료한다.

**이번 실행에서 새 브라우저·스크린샷·프로필은 생성하지 않았다.** 필수 `external-ai-orchestrator/SKILL.md`의 실제 위치를 아직 확인하지 못했다. 해당 경로/파일은 사용자에게 요청했다. 이전 실행의 14:32 스크린샷을 v4 재검증 결과로 재사용하지 않는다.

## 4. 공개 재검증의 실제 차단

준비한 독립 읽기 전용 감사: `tmp/pc-agent2-resume-public-audit.mjs`.

실행을 요청한 명령:

```powershell
node tmp/pc-agent2-resume-public-audit.mjs *> tmp/pc-agent2-resume-public-audit-20260917.log
```

연결 도구는 실행 전에 다음을 반환했다.

> 요청의 보안 상태를 결정하지 못해 이 도구 요청은 OpenAI에 의해 차단되었습니다.

프로세스 세션·exit code·HTTP 상태·새 공개 응답을 받지 못했다. 이는 사이트의 503이나 SSH 키 문제를 확인한 결과가 아니다. 2026-09-17 후속 실행 중의 거부이며, 거부 응답에 없는 정확한 초단위 시각은 만들지 않았다. 다른 도구·셸·스크립트로 요청을 우회하지 않았다.

루트 검증 종료 후 후보 해시 비교와 diff 재검사를 묶은 요청도 같은 원문으로 실행 전에 거부됐다. 해시 비교 파일을 새로 생성했다고 주장하지 않는다. 최초 후보 해시 저장과 앞선 diff 검사의 성공은 별개로 유지한다.

따라서 **이번 후속 실행에는 새 공개 API 가격, 새 9종 화면 합계, v4 배포 일치 판정이 없다.** 1번 결과와 일반 릴리스 문서에 있는 최종 게시/배포 기록은 인수 자료로 읽었지만 그것을 이번 UI 후보의 직접 운영 검증으로 확장하지 않는다.

## 5. 변경 파일과 인수용 해시

다음은 2026-09-17 **15:10:20 KST**에 실제 저장한 후보 해시다. 이후 루트 검증은 exit 0으로 종료했으나 검사 후 해시 재조회는 거부됐다. 통합 담당자는 실제 최신 파일을 다시 대조해야 한다.

| 앱 상대 경로 | SHA-256 |
|---|---|
| `web-backend/public/pc-tools.js` | `71ac06729b84a967644414f3c79674d638e544472ab91c4e768d6e7a68844c1e` |
| `web-backend/public/pc-tools-core.mjs` | `fc72c41823c113adcf1d72f88f5f1d7dc9f76c62141022061ff3f679b17b1147` |
| `web-backend/public/pc-tools-data.mjs` | `51ae10da4570ab6d75c93477f3b1efe1289b5e9b1e0e7390141b5d444cc73c71` |
| `web-backend/public/pc-tools.css` | `26b661427b3716d6beaf62cf746d98b2630c226442d46c7e81d76544b2072b10` |
| `web-backend/public/computer-builder.html` | `e7deb85a6d964f1217486e8316b09bc8edc913a3e2296b882a6df893285867ae` |
| `web-backend/public/price-analysis.html` | `316e206434e3d93650a6b893ce3d52a37705df660307cf165e34bb49b8ef26b9` |
| `harness/pc-agent2-ui-contract.mjs` | `9314f9b733fc2788688681258ac7628dc4269f6e695db9714bf4b1d2a2b67955` |
| `harness/pc-agent2-resume-contract.mjs`（신규） | `bb7067732790b1f70fcf4929e2521a9bba0814ef2763b7a170812b61f36cd38c` |
| `tmp/pc-live-v18-browser.mjs` | `45322b3717c30a75c1c95f38d30deb8e219d28a0b951f4c5bed1794037020b71` |
| `tmp/pc-agent2-resume-public-audit.mjs`（신규） | `af96d13ae880125217f1b764f9874aba514b48b32d574ef3edcb720de76e593c` |

추가 문서: 이 결과, `02-agent2-resume-progress.md`, 이전 `02-agent-categories-ui-browser-result.md`의 후속 결과 안내. 기존 공유 백엔드·분류·master·원장·package.json 파일은 수정하지 않았다. tmp 스크립트·로그·이미지·운영 응답을 일괄 커밋하지 않는다.

## 6. 최종 통합에 남은 사항

통합 담당자는 다른 작업자의 변경과 위 10개 파일의 현재 해시를 대조하고, 역할 2의 두 하네스를 최종 검사 연결에 포함한다. v4 HTML/import/CSS를 함께 정식 배포한 뒤 일반 URL의 자산 일치와 9종·10개 수량 독립 브라우저 검사를 수행해야 한다. 운영 배포나 통합 커밋은 이 역할이 대신 실행하지 않았다.

사용자 측 필요한 것은 실제 `external-ai-orchestrator/SKILL.md` 위치/내용과 승인된 읽기 전용 검사 요청을 실행할 수 있는 연결 상태다. 인증키·토큰 공개, WAF 전체 해제, 유료 전환은 필요하다고 판단한 적이 없다.

**이번 종료 상태: 후속 UI 소스 적용 및 로컬 검증 PASS / 공개 재조회 BLOCKED / 직접 브라우저 미실행 / 운영 반영 확인 미완료.**
