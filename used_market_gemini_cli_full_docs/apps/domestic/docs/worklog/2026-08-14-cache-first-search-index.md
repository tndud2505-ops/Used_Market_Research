# 2026-08-14 캐시 우선 검색 색인 작업 기록

## 구현

- AWS Runner에 SQLite WAL·FTS5 기반 검색 색인을 추가했다.
- 정규화 검색어와 사이트·카테고리만 색인 키로 사용하고 가격·정렬은 조회 조건으로 분리했다.
- 신규·변경 upsert, 가격 이력, 2회 누락 비활성화, 사이트 실패 보존, 6시간 만료, 14일/30일 정리를 구현했다.
- 인기 15분·일반 60분·단발 요청 시 갱신과 하루 1회 깊은 수집을 구현했다.
- 갱신 토큰 조회와 운영자 색인 상태 API를 추가했다.
- 화면에 확인 시각·갱신 중·신규 매물·오래된 결과 선택 상태를 추가했다.
- Worker는 AWS 응답을 다시 조립하지 않고 색인 결과를 전달하며, AWS 실패 때만 D1을 사용한다.
- D1 import 허용 대상에 당근을 추가하고 사이트별 2,000개로 정리한다.
- systemd 메모리 제한과 `/var/lib/used-market-runner` 상태 디렉터리를 추가했다.
- shadow 비교 모드에서 성공한 사이트의 누락률·오래된 결과율을 24시간 집계하도록 했다.

## 로컬 검증

- 색인 정책 계약: 35개 통과
- 화면 갱신 계약: 10개 통과
- Cloudflare Worker 계약: 27개 통과
- 100,000개 SQLite 부하:
  - DB 크기: 88,969,216 bytes
  - 단일 조회 4종: 102.55~127.28ms
  - 동시 사용자 4명 wall time: 220.68ms
  - 동시 조회 개별: 176.96~186.21ms
  - 측정 프로세스 RSS: 347,729,920 bytes

대량 테스트의 첫 시도는 실제 수집용 해시·FTS 경로로 100,000개를 한 건씩 준비해 5분 제한을 넘겼다. 조회 부하 검증의 목적에 맞게 데이터 준비만 SQLite 벌크 적재로 바꿨으며, 실제 수집 단위의 upsert·중복·가격 변경은 별도 정책 계약으로 검증했다.

## 운영 전환 기록

- 첫 배포 모드: `shadow`
- 24시간 확인 항목: `/api/index/status`의 `comparison`, DB·디스크·메모리·갱신 지연 경고
- cache-first 전환 기준과 장애 대체 정책: `docs/decisions/ADR-cache-first-search-index.md`

2026-08-14 14:20 KST 배포·초기 검증:

- AWS `used-market-runner.service`, `used-market-tunnel.service`: active
- AWS 배포 모드: `shadow`
- 배포 전 여유 디스크: 52GB, 배포 후 측정 약 51.4GB
- systemd: `MemoryHigh=2560M`, `MemoryMax=3072M`
- Worker 버전: `4825d96d-011f-46cc-9586-c3b51ee54f11`
- Worker·AWS 배포 전 백업: `/var/backups/used-market-runner/20260814T141500KST`
- 5개 사이트 `아이폰 15` 실검색: 24개 응답, 색인 후보 113개
- `iPhone15`도 원 사이트 수집어 `아이폰 15`로 통일한 뒤 같은 5개 사이트 결과 확인
- clean shadow 초기 7회·6개 검색어: 누락률 0%, 오래된 결과율 약 0.92%
- 비교 포함 사이트: 번개장터·당근·헬로마켓·중고나라·리씽크몰
- 비교 포함 카테고리: 전체·가전·패션·게임·모바일·PC
- 현재 활성 상품 474개, DB 2,146,304 bytes, RSS 141,230,080 bytes
- DB·디스크·메모리·갱신 지연 경고: 모두 false
- D1 import 경계 설정 및 초기 시드 완료, 현재 일일 쓰기 474개·거절 0개
- 공개 브라우저: 111개 결과·5개 출처, 가격 범위 30만~60만원 위반 0, 낮은 가격 오름차순, 모바일 가로 넘침 0, 콘솔 오류·경고 0
- 최종 공개 API 확인: `live_compare`/`shadow`, 40개 응답, 확인 시각 정상 반환
- 2026-08-15 14:50 KST부터 매일 ADR 전환 기준을 확인하는 작업 자동화를 연결했다. 기준 미달이면 shadow를 유지하고, 전부 통과할 때만 `cache_first`로 전환·재검증한다.

`cache_first`는 아직 켜지 않았다. 24시간 shadow 지표와 카테고리·사이트 분포가 ADR 기준을 통과한 뒤 전환한다.

## 2026-08-14 14:32 KST 자동 검증

- 배포 후 약 12분밖에 지나지 않아 24시간 조건 미충족으로 `shadow`를 유지했다.
- AWS 러너·터널은 `active`, 최근 20분 서비스 오류 0건, DB·디스크·메모리·갱신 지연 경고는 모두 false였다.
- 초기 상태는 비교 8회·검색어 6개, 5개 사이트와 전체·가전·패션·게임·모바일·PC 포함, 누락률 약 1.06%, 오래된 결과율 약 0.91%였다.
- 추가 표본에서 당근·헬로마켓의 최근 상위 결과가 호출마다 크게 바뀌는 현상을 확인했다. 기존 비교식은 6시간 이내 매물이 한 번 보이지 않아도 오래된 결과로 계산해 2회 누락·6시간 정책과 충돌했다.
- 비교식은 live에서 빠진 색인 매물 중 마지막 확인이 6시간을 넘은 항목만 오래된 결과로 계산하도록 수정했다.
- `refresh_index=false`이고 색인이 신선하면 shadow에서도 SQLite 결과만 반환하도록 수정했다. 가격·정렬 확인에서 `mode=index`, 40개 반환, 비교 실행 수 15→15로 유지되어 원본 재수집이 없음을 확인했다.
- 수정 후 실제 비교 행: live 147개, 색인 147개, 누락 6개(약 4.08%), 오래된 결과 0개.
- 수정 파일 배포 전 백업: `/var/backups/used-market-runner/20260814T144500KST-heartbeat`
- 수정 배포 후 AWS 러너·터널 `active`, `shadow`, 오류 0건이다.
- 현재 누적 비교는 16회·13개 검색어다. 수정 전 진단용 비교 행이 섞여 누적 누락률 약 9.49%, 오래된 결과율 약 8.87%이므로 전환 판단에 사용하지 않는다. 24시간 창에서 이 행들이 빠지는 2026-08-15 14:50 KST 이후 새 계산식 표본으로 판단한다.

## 2026-08-14 14:51 KST 자동 검증

- 배포 후 약 31분으로 24시간 조건을 충족하지 않아 `shadow`를 유지했다.
- 인증된 공개 `/api/index/status`는 HTTP 200, AWS 러너·터널은 `active`, 최근 15분 서비스 오류 0건이었다.
- 전체·가전·패션·게임·모바일·PC에서 5개 사이트 비교 표본을 각각 2회씩 수집했다. 모든 응답은 `live_compare`였고 갱신 시각이 존재했다.
- 14:45 KST 이후 첫 6회는 live 461개, 누락 70개(15.18%), 오래된 결과 0개였다.
- 신규 매물을 병합한 뒤 두 번째 6회에서는 live 454개, 누락 7개(1.54%), 오래된 결과 0개로 수렴했다. 아이폰15·다이슨·나이키·닌텐도 스위치·갤럭시 S24는 누락 0개였고 맥북만 7개였다.
- 새 계산식 표본은 총 12회로 최소 30회 조건에 미달한다. 누적값은 첫 수집 영향을 포함해 누락률 약 8.42%, 오래된 결과율 0%다.
- 현재 활성 상품 707개, DB 2,846,720 bytes, RSS 147,460,096 bytes, 여유 디스크 약 55.1GB, 대기·갱신 지연 0이며 모든 경고가 false다.
- 다음 24시간 점검 전까지 전환하지 않고 실제 검색과 예약 갱신 표본을 더 누적한다.

## 2026-08-14 검색 페이지 구조 개선

- 공개 비교 확인 결과 중고닷은 `아이폰` 첫 검색에 약 35초가 걸린 뒤 약 3,191개 메타데이터를 확보했고, 화면에는 20개씩 추가 표시했다. 낮은 가격순 전환은 약 0.8초였으나 500원짜리 액세서리가 첫 결과로 나와 전체 수량 대비 노이즈가 확인됐다.
- 중고나라는 카테고리 19에서 페이지당 상품 링크 50개와 URL 기반 페이지·정렬 파라미터를 사용했다. 실시간 데이터 이동과 광고성 결과 영향으로 1·2페이지 사이 상품 ID 중복도 확인됐다.
- 우리 서비스는 수천 개를 브라우저에 미리 전달하지 않는다. SQLite에 사이트별 최대 160개·전체 최대 800개를 저장하고 브라우저에는 30개씩 전달하는 서버 페이지네이션으로 확정했다.
- `price + item_id` 또는 `posted_at + item_id` 계열의 keyset과 스냅샷 버전을 HMAC 서명한 `index:v2` 커서를 추가했다. 서버 재시작 후에도 다음 페이지가 이어지고, 정렬·가격·검색 조건이 바뀐 커서는 거부한다.
- 스냅샷은 최대 1,000개·6시간이며 상품 표시값과 정렬값을 함께 고정한다. 신규 매물·가격 변경·100,000개 전역 정리 중에도 이미 보던 페이지의 총량·순서·가격이 바뀌지 않는다.
- 첫 응답 뒤 신규 매물이 들어와도 기존 스냅샷의 다음 페이지에는 끼어들지 않고, 새 검색에서만 새 스냅샷으로 보인다.
- 가격·정렬 변경은 기존 커서를 버리고 SQLite에서 새 1페이지를 조회한다. 원 사이트 재수집은 하지 않는다.
- 내부 수집 보존량을 기존 사이트별 40개·전체 200개에서 사이트별 160개·전체 800개로 늘렸다. 전송 단위는 30개라 브라우저 메모리와 초기 응답은 작게 유지한다.
- 사용자용 출처 상태에서 단순 필터 경고를 `부분 실패`로 표시하지 않도록 수정했다. 실제 오류는 `일부 확인`, 당근 지역 제한은 `일부 지역`, 조건 필터는 `조건 적용`으로 구분한다.

코드 리뷰에서 발견한 배포 차단 4건과 조치:

- 갱신 뒤 기존 스냅샷의 표시 가격이 달라지는 문제: 스냅샷에 표시 상품 JSON을 고정 저장하도록 수정했다.
- 전역 100,000개 정리가 유효 스냅샷을 연쇄 삭제하는 문제: 스냅샷 상품을 활성 상품 FK와 분리했다.
- `shadow`가 색인 결과를 사용자에게 주던 문제: live 응답 경로를 유지하고 `cache_first`에서만 SQLite 페이지를 제공하도록 복원했다. 14:32 기록의 shadow SQLite 전용 동작은 이 변경으로 폐기됐다.
- 20,000개 이상에서 정리가 급격히 느려지는 문제: `query_listings(item_id, missing_count)` 인덱스를 추가했다.

최종 로컬 검증:

- 전체 `npm test`: 통과
- 색인·커서·화면 계약: 39개 + 31개 + 10개 통과
- 실시간 검색 계약: 127개 통과
- 100,000개 부하: 스냅샷 갱신 94ms, 단일 조회 1.06~1.67ms, 동시 사용자 4명 wall time 38.57ms
- 100,000개 부하 DB 84,389,888 bytes, RSS 172,355,584 bytes
- 기존 스냅샷 가격 고정, 신규 매물 미삽입, 전역 정리 후 페이지 유지, 서버 재시작 후 커서 재개를 별도 회귀 테스트로 확인했다.

이 변경 배포도 `RUNNER_INDEX_MODE=shadow`를 유지한다. 24시간·30회·5개 사이트·품질 기준을 모두 통과하기 전에는 `cache_first`로 전환하지 않는다.

## 2026-08-14 19:05 KST 배포·실사용 검증

- AWS 스냅샷 커서 런타임과 Cloudflare UI를 실제 배포했다. 최종 Worker 버전은 `6b93888c-714c-42a4-aec9-997dc8c2ed94`다.
- AWS 백업:
  - `/var/backups/used-market-runner/20260814T184412+09:00ST-snapshot-cursor`
  - `/var/backups/used-market-runner/20260814T190012-quality-filter`
  - `/var/backups/used-market-runner/20260814T190247-ad-filter`
- AWS 러너·터널은 `active`, 모드는 `shadow`, 최근 서비스 오류 0건이다. DB 14,729,216 bytes, 활성 상품 2,000개, 검색어 20개, 활성 스냅샷 18개·2,981행, 대기 갱신 0개, 운영 경고 0개다.
- 공개 `아이폰 15` 추천순 첫 요청은 30개를 반환했고 전체 검증 결과는 356개였다. 사이트별로 중고나라 38·번개장터 20·당근 150·헬로마켓 117·리씽크몰 31개가 확인돼 기존 사이트당 40개 제한을 실제로 넘겼다.
- 공개 API 2페이지는 307ms에 30개를 반환했고 1페이지와 중복은 0개였다.
- 공개 UI는 30개 카드와 7페이지를 표시했다. Playwright에서 2페이지 클릭 후 `2/7페이지`, 두 번째 `POST /api/search`, `limit:30`, `site_window:160`, continuation cursor를 확인했다. 콘솔 오류·경고는 0개였다.
- `부분 실패` 문구는 공개 화면에서 사라졌다. 정상 필터링은 `조건 적용`, 실제 사이트 오류만 `일부 확인`으로 구분한다.
- 낮은 가격순 실측에서 처음 남아 있던 500원 교환글·3천원 박스·배터리·카메라 부품·차저·모델 불일치·재고정리 광고를 하드 필터에 추가했다.
- 재배포 후 `iPhone15` 낮은 가격순은 30개, 전체 179개, 가격 오름차순을 유지했고 첫 30개에서 위 알려진 노이즈는 0개였다. 첫 가격은 291,500원이었다.
- 운영 DB 복사본을 별도 포트의 `cache_first`로 실행한 통합 검증:
  - 첫 30개 `mode=index`, `index:v2` 서명 커서, 스냅샷 버전 2
  - 프로세스 재시작 뒤 다음 30개 정상, 페이지 중복 0개
  - 낮은 가격순 변경도 `mode=index`, 비교 실행 수·`refreshed_at` 불변으로 원본 재수집 없음
  - 갱신 토큰은 `queued`로 시작해 polling HTTP 200·`completed`·30개 병합 결과로 끝남
- Worker 계약에서 AWS 500 응답 시 D1 장애 대체를 포함한 27개 검사가 통과했다.

19:05 배포를 기준으로 잡았던 최초 24시간 시각은 아래 19:23 교정 배포로 폐기했다.

## 2026-08-14 19:23 KST 카테고리 회수율 교정

- 최종 배포 이후 첫 카테고리 표본에서 전체·게임·PC는 정상인 반면 `갤럭시 S24` 모바일, `다이슨 에어랩` 가전, `나이키 운동화` 패션의류가 0건으로 나타났다.
- `나이키 운동화`는 패션의류가 아니라 패션잡화 대상이어서 검증 검색어를 `나이키 후드티`로 바로잡았다.
- 실제 결함은 두 가지였다. 원 사이트 검색어 정규화가 `S24`, `M2`, `V10`을 `S 24`, `M 2`, `V 10`으로 쪼갰고, 가전 분류 별칭에 에어랩·드라이기 등 소형 생활가전이 없었다.
- 모델명은 한 글자 영문+숫자를 붙여 유지하고 `S 24` 입력도 `s24`로 통일했다. 가전 별칭에는 에어랩·드라이기·헤어드라이어·고데기·공기청정기·제습기·건조기·스타일러·에어프라이어·커피머신을 추가했다.
- 기존 DB에 저장된 `갤럭시 s 24` 같은 검색어도 다음 검색 시 교정된 값으로 갱신해 백그라운드 수집이 과거 문자열을 재사용하지 않도록 했다.
- 회귀 테스트는 수정 전 두 경로에서 실패하는 것을 확인한 뒤 추가했고, 수정 후 색인 계약 39개·실시간 검색 계약 127개와 전체 `npm test`가 통과했다.
- AWS 배포 전 백업:
  - `/var/backups/used-market-runner/20260814T101857+0000-model-category-recall`
  - `/var/backups/used-market-runner/20260814T102214+0000-stored-keyword-repair`
- Cloudflare 최종 Worker 버전은 `60e85867-8ea5-4b66-8fa1-f7f11ec3463a`다. 양 공개 도메인의 health와 32개 카테고리 응답이 통과했다.
- 재검증 결과 `갤럭시 S24` 모바일은 30개/전체 130개, `다이슨 에어랩` 가전은 30개/전체 115개, `나이키 후드티` 패션의류는 30개/전체 192개를 반환했다. 모바일·가전은 첫 페이지에 5개 사이트가 모두 포함됐다.
- 전체·가전·패션의류·게임·모바일·PC, 5개 사이트를 포함한 최종 배포 이후 표본은 현재 14회다. live 2,002개 대비 누락 145개(7.24%), 오래된 상품 0개(0%)다. 두 번째 수집의 가전·패션·게임·모바일·PC 누락은 모두 0개였고, 전체 `아이폰 15`의 원본 변동이 남아 누적 기준 5%를 아직 넘는다.
- 운영 모드는 계속 `shadow`다. 자동 검증은 AWS SQLite의 `comparison_runs.created_at >= 2026-08-14T10:23:00.000Z` 행만 집계하고 4시간 간격으로 카테고리별 최대 1회씩 분산 표본을 보강한다.
- 가장 이른 전환 판단 시각은 2026-08-15 19:30 KST다. 그때까지 24시간·비교 30회·5개 사이트·주요 카테고리·누락률 5% 이하·오래된 상품률 10% 이하·운영 경고 없음·거짓 `refreshed_at` 없음이 모두 충족돼야 `RUNNER_INDEX_MODE=cache_first`로 변경한다.

## 2026-08-14 19:33 KST shadow 비교 상한 교정

- 19:23 배포 이후 누락률 7.24%가 실제 누락인지 운영 DB와 공개 검색 전체 페이지를 대조했다.
- `아이폰 15` 가격 조건 검색의 live 고유 상품 154개는 모두 현재 색인에 연결돼 있었고, 154개 모두 마지막 비교 실행 전부터 저장된 상품이었다. 해당 검색의 활성 연결 상품은 268개였다.
- shadow 비교 코드가 색인 상위 200개만 조회해, 200개 밖에 있던 live 상품을 DB에 없는 것으로 오판했다. 사용자 전송 30개와 비교 검증 범위는 별개인데 이전 200개 제한이 남아 있던 것이 원인이었다.
- 비교용 색인 조회를 `SEARCH_COLLECTION_MAX_ITEMS`와 같은 800개로 변경했다. 사용자 응답은 계속 30개씩 전달한다.
- 회귀 테스트에서 200개 제한 경로가 수정 전 실패하고 수정 후 전체 수집 창을 사용하는 것을 확인했다. 전체 `npm test`와 구문 검사가 통과했다.
- AWS 배포 전 백업: `/var/backups/used-market-runner/20260814T103222+0000-full-shadow-comparison`
- 배포 후 러너·터널은 `active`, 운영 모드는 `shadow`, 최근 서비스 오류 0건이다.
- 수정 후 전체·가전·패션의류·게임·모바일·PC 각 1회, 5개 사이트 포함 표본은 6회다. live 798개, 색인 957개, 누락 8개(1.00%), 오래된 상품 0개(0%)로 확인됐다.
- 이전 상위 200개 계산 행과 19:23 전환 시각은 최종 판단에서 제외한다. 자동 검증은 `comparison_runs.created_at >= 2026-08-14T10:32:40.000Z`만 집계한다.
- 가장 이른 전환 판단 시각은 2026-08-15 19:40 KST다. 자동 검증은 4시간마다 6개 카테고리 표본을 한 차례씩만 보강하고, 24시간·30회·품질·자원 기준을 모두 충족할 때만 `cache_first`로 전환한다.

## 2026-08-14 19:45 KST 번개장터 단일 사이트 회수율 교정

- 번개장터만 선택해 `아이폰 15`를 검색하면 8개만 노출되는 원인을 확인했다. `site_window=160`이어도 번개장터 검색 API는 페이지당 20개·첫 페이지만 요청했고, 그 안에서 품질 필터를 적용한 결과만 남긴 구현 결함이었다.
- 원본 API를 직접 확인했을 때 0~7페이지가 각각 20개였고 160개 상품 ID는 모두 고유했다. 원본 재고 부족이 아니라 우리 페이지 수 계산 문제였다.
- 요청 후보 수에 맞춰 필요한 페이지만 조회하도록 바꿨다. 40개 후보는 최대 2페이지, 초기 색인 160개 후보는 최대 8페이지이며 목표 수량을 채우거나 원본 마지막 페이지를 만나면 조기 중단한다.
- 로컬 수집에서 `아이폰 15` 원본 160개 중 84개가 키워드·카테고리·구매 가능성 필터를 통과했다. 회귀 검사 4개를 추가했고 실시간 검색 계약 131개와 전체 `npm test`가 통과했다.
- AWS 배포 전 백업: `/var/backups/used-market-runner/20260814T104246+0000-bunjang-pagination`
- Cloudflare Worker 버전: `ce6e6644-30e9-4e89-ac13-3fcacd8b6ee3`. 양 공개 도메인의 health와 32개 카테고리 응답을 확인했다.
- 공개 단일 사이트 실검색 결과:
  - 번개장터: 첫 30개, 전체 61개, 다음 30개, 페이지 중복 0개
  - 당근: 첫 30개, 전체 110개, 다음 30개, 페이지 중복 0개
  - 중고나라: 첫 30개, 전체 33개, 다음 3개, 페이지 중복 0개
  - 헬로마켓: 품질 검증 통과 26개
  - 리씽크몰: 품질 검증 통과 2개
- 헬로마켓·리씽크몰의 부족분은 액세서리·광고·다른 모델을 섞어 40개를 강제로 채우지 않는다. 선택 사이트에 검증된 매물이 30개를 넘으면 서버 페이지네이션을 제공하고, 실제 후보가 적으면 정확한 수량만 표시한다.
- 운영 상태는 `shadow`, DB 30,760,960 bytes, 활성 상품 2,844개, RSS 109,789,184 bytes, 여유 디스크 약 54.1GB, 대기 갱신·갱신 지연 0이며 DB·디스크·메모리·갱신 지연 경고가 모두 false다.
- 최종 공개 배포 기준 시각을 2026-08-14 19:45 KST로 다시 잡았다. 자동 검증은 `comparison_runs.created_at >= 2026-08-14T10:45:00.000Z` 행만 사용하며, 가장 이른 전환 판단 시각은 2026-08-15 19:50 KST다.

## 2026-08-14 21:01 KST 낮은 가격순 구매 품질 교정

- 배포 전 공개 `아이폰 15` 낮은 가격순은 30개를 반환했지만 8,000원짜리 `핸드폰 제외 판매`가 첫 상품이었고, 검증기는 `invalid_price_asc_order`, `purchase_noise_in_top_10`으로 실패했다.
- 가격만 오름차순으로 정렬하지 않고 품질 등급을 먼저 적용하도록 변경했다. 등급은 정상 검증, 미평가·주의, 구매 불가 노이즈·사기 위험도 0.45 초과, 가격 없음·표시용 가격 순이며 같은 등급 안에서 가격과 상품 ID를 사용한다.
- 가격 의심·품질 의심·노이즈·사기 위험도는 전역 `listings`가 아니라 `query_listings`에 저장한다. 한 검색어에서 액세서리로 판단한 상품이 다른 검색어에서도 전역 불량으로 오염되지 않도록 했다.
- 기존 DB 마이그레이션은 `query_snapshots`를 삭제하지 않고 고정 상품 JSON과 정렬 키를 새 스냅샷 항목 테이블로 복사한다. 운영 배포에서 스냅샷 102개와 항목 14,199개가 배포 전후 동일했고 `PRAGMA integrity_check=ok`, `user_version=3`, 품질 컬럼 6개를 확인했다.
- 로컬 검증은 전체 `npm test`, 색인 77개·커서 31개·운영 계약 19개·프로세스 통합 16개·UI 10개·실시간 검색 131개가 통과했다. 독립 재검토에서도 배포를 막는 P1/P2가 없었다.
- AWS 최종 백업: `/var/backups/used-market-runner/20260814T115654Z-query-quality-rank`
- 배포 중 러너 중지가 터널도 함께 중지하는 systemd 의존관계를 확인했다. 중단된 러너·터널을 복구했고, 최종 배포 절차는 두 서비스를 명시적으로 재시작하고 로컬·공개 health를 확인하도록 교정했다.
- AWS 최종 상태는 러너·터널 `active`, `shadow`, DB 46.3MB, 활성 상품 3,720개, 검색어 40개, 대기·갱신 지연 0, 여유 디스크 50.3GB, RSS 181.6MB, 운영 경고 0개다.
- Cloudflare Worker 버전은 `38242779-e78b-4a52-b854-4f91ede6905f`이며 양 공개 도메인의 health와 32개 카테고리 응답이 통과했다.
- 배포 후 `아이폰 15` 낮은 가격순과 30만~90만 원 가격 범위 요청은 모두 30개를 반환하고 검증을 통과했다. 무제한 검색은 전체 230개·5개 사이트를 포함했고 첫 가격은 492,000원, 상위 10개 구매 불가 노이즈는 0개였다.
- 패션·모바일·PC·게임·가전·카메라·뷰티 8개 공개 시나리오가 모두 통과했다. 브라우저 전송은 첫 30개로 유지하고 전체 검증 후보는 서버 색인에 저장한다.
- 24시간 누적 상태는 비교 63회·5개 사이트·주요 카테고리를 포함하고 오래된 상품률 약 1.26%, 경고 0개지만, 배포 전 행이 섞인 누락률 약 12.36%는 전환 근거로 사용하지 않는다.
- 최종 전환 표본 기준은 `comparison_runs.created_at >= 2026-08-14T11:59:00.000Z`다. 운영 모드는 계속 `shadow`이며 가장 이른 전환 판단 시각은 2026-08-15 21:05 KST다. 그때까지 새 표본 30회·5개 사이트·주요 카테고리·누락률 5% 이하·오래된 상품률 10% 이하·운영 경고 없음·거짓 `refreshed_at` 없음을 모두 확인한다.
- 새 기준 시각 직후 SQLite 비교 행은 0회다. 공개 시나리오 검증은 러너·Worker 캐시를 포함할 수 있으므로 비교 실행 수로 부풀리지 않고, 3시간 이상 간격을 둔 실제 shadow 수집만 자동 검증이 누적한다.
- 완료 감사에서 서버는 최대 800개를 색인하지만 UI의 `RESULT_WINDOW_MAX`가 이전 수집 상한 200에 남아 있는 불일치를 발견했다. 첫 응답 30개는 유지하고 사용자가 페이지를 요청할 때 최대 800개까지 이어서 받을 수 있도록 상한을 맞췄다.
- UI 페이지 계약은 기존 200 상한에서 실패하는 회귀 검사를 먼저 추가했고, 수정 후 19개 검사와 전체 `npm test`가 통과했다. 새 Worker 버전은 `ff48f8eb-3f3d-43ac-9827-94282a8075fb`다.
- 공개 브라우저에서 `아이폰 15` 추천순은 총 232개·8페이지, 낮은 가격순은 총 310개·11페이지로 표시됐다. 각각 마지막 페이지까지 이동해 추천순 22개, 가격순 마지막 페이지를 정상 렌더링했고 `CURSOR_EXPIRED`·콘솔 오류·경고는 없었다.
- 낮은 가격순 전환은 8페이지에서 기존 커서를 버리고 `1/11페이지`로 돌아갔으며 첫 가격은 492,000원부터 495,000원·500,000원·520,000원 순이었다. UI 상한 교정은 shadow 비교 계산과 AWS 색인 코드에 영향을 주지 않으므로 `2026-08-14T11:59:00.000Z` 품질 게이트 기준은 유지한다.
- 브라우저 실사용 검증으로 새 기준 이후 비교 2회가 자연 누적됐다. live 275개·색인 355개·누락 15개(5.45%)·오래된 상품 24개(6.76%)다. 누락률은 기준보다 0.45%p 높고 실행 수도 2/30이므로 `shadow`를 유지하며 다음 분산 표본에서 수렴 여부를 확인한다.

## 2026-08-14 21:32~21:44 KST 검색 조작 경쟁·shadow 계약·D1 대체 품질 배포

- 실제 브라우저에서 첫 검색이 끝나기 전에 낮은 가격순을 누르면 진행 중 요청을 취소하고 D1 대체 결과로 넘어갈 수 있는 경쟁 조건을 재현했다. 검색 중에는 정렬 탭·최소/최대 가격·적용·초기화 버튼을 비활성화하고 각 이벤트에도 `state.loading` 방어를 추가했다.
- UI 결과 창은 서버와 같은 최대 800개를 유지하며, 페이지 수가 많을 때 처음·현재 주변·마지막 페이지만 보이는 축약 페이지네이션을 적용했다. 캐시 버전은 `search-controls-v4`다.
- ADR과 달리 `shadow`에서 `refresh_index=false`이고 색인이 신선하면 사용자에게 색인 응답과 `offset:v1` 메모리 커서를 주던 분기를 제거했다. `shadow`는 다시 항상 live 비교 응답을 주고, SQLite 전용 `index:v2` 커서는 품질 게이트 통과 후 `cache_first`에서만 사용한다.
- D1 대체 검색에도 live와 같은 구매 요청·교환·광고·키워드 불일치·액세서리·의심 가격 필터와 품질 정렬을 적용했다. 공백·하이픈·밑줄 등이 다른 모델명도 D1 SQL 후보 검색에서 찾도록 compact 검색식을 추가했고, 검색어가 있는 카테고리 요청은 헬로마켓·리씽크몰 같은 키워드형 사이트를 잘못 거부하지 않게 교정했다.
- 전체 `npm test`가 통과했다. 색인 77개, 커서 31개, 운영 전환 계약 19개, 프로세스 통합 16개, 실시간 검색 품질 131개, UI 30개 계약을 포함한다. Cloudflare 회귀 검사는 35개가 통과했다.
- AWS 배포 전 백업은 `/var/backups/used-market-runner/20260814T123201Z-shadow-live-contract`다. 배포 후 러너·터널은 모두 `active`, `RUNNER_INDEX_MODE=shadow`, 배포 파일 해시 일치, DB 53,755,904 bytes, 활성 상품 3,854개, 대기·갱신 지연·DB·디스크·메모리 경고 0개다.
- 최종 Cloudflare Worker 버전은 `479a462d-4ba6-45be-8a95-4f79d1191b63`이다. `used-pick.com`과 `www.used-pick.com` health 및 32개 카테고리 응답이 통과했다.
- 공개 브라우저에서 `아이폰 15` 검색 중 정렬·가격 조작이 잠기고 완료 후 다시 활성화되는 것을 확인했다. 추천순은 총 230개·8페이지, 낮은 가격순은 총 231개·8페이지였고 첫 가격은 492,000원·495,000원·500,000원·500,000원·520,000원 순이었다. 상단에 매입·교환·박스만·본체 제외 광고는 없었다.
- 운영 검색 검증 8개 시나리오가 모두 통과했다. `iphone-lowest`는 5개 사이트, 30개 반환, 전체 후보 227개, 첫 가격 492,000원, 상위 구매 불가 노이즈 0개였다.
- 품질 통과 상품 1,148개를 D1에 재시드했고 거부 0개였다. UTF-8 실제 `아이폰 15` D1 장애 대체 요청은 원본 사이트 호출 없이 30개를 반환했으며 첫 가격은 490,000원·492,000원·495,000원·500,000원·500,000원, 상위 10개 구매 불가 노이즈 0개였다.
- 최종 품질 기준 시각 `2026-08-14T11:59:00.000Z` 이후 SQLite 직접 집계는 14회, live 2,303개, 색인 2,673개, 누락 186개(8.08%), 오래된 상품 90개(3.37%)다. 5개 사이트와 전체·가전·뷰티·카메라·패션·게임·모바일·PC를 포함하지만 실행 14/30이고 누락률이 5%를 넘으므로 `shadow`를 유지한다.
- 이번 AWS 변경은 누락률 계산식·검색어별 품질 등급·스냅샷 데이터 자체를 바꾸지 않고 잘못 노출되던 shadow 읽기 분기만 제거했다. 따라서 품질 게이트 기준 시각은 `2026-08-14T11:59:00.000Z`, 가장 이른 전환 판단은 2026-08-15 21:05 KST로 유지한다.

## 2026-08-14 22:10~22:24 KST 중고나라 RAM·카테고리 회수율과 페이지 안정성 교정

- 공개 단일 사이트 `RAM` 검색이 6개만 나오던 원인은 중고나라 원본 50개 중 영문 `RAM`이 제목에 그대로 있는 6개만 통과하고 한글 `램` 매물을 키워드 불일치로 버린 것이었다.
- `RAM`과 `램`을 같은 PC 메모리 의도로 판정하되 `램스킨`, `램프`, `슬램덩크`, `글램팜`, `램 레더` 같은 합성어 오탐은 제외했다. DDR·SODIMM·UDIMM·PC4/PC5와 용량이 함께 있는 메모리 제목도 RAM 검색에 포함했다.
- 중고나라 검색과 단일 공식 카테고리는 페이지당 50개를 기준으로 초기 `site_window=160`에서 최대 4페이지를 읽도록 바꿨다. 로컬 원본 확인에서 `RAM` 200개 중 관련 후보 90개, `노트북/PC` 카테고리 200개 중 최대 160개 후보를 확보했다.
- 공개 배포 후 중고나라 `RAM`은 첫 30개·전체 84개·3페이지, `노트북/PC` 카테고리는 첫 30개·전체 148개(후속 실시간 표본 144개)·5페이지로 확인됐다. RAM 상위 30개에서 알려진 합성어 오탐은 0개였다.
- 브라우저에서 PC 카테고리 5/5페이지와 RAM 3/3페이지까지 이동했으며 페이지 만료·중복 오류와 콘솔 오류·경고는 없었다.
- 첫 검증에서 백그라운드 색인 갱신 완료 시 `searchCache.clear()`가 모든 진행 중인 shadow live 페이지 창을 지워 `CURSOR_EXPIRED`를 만드는 결함을 재현했다. 전역 삭제를 제거해 갱신과 사용자의 5분 페이지 창을 분리했고 회귀 검사를 추가했다.
- 전체 `npm test`와 색인 회귀 검사 78개가 통과했다.
- AWS 백업:
  - `/var/backups/used-market-runner/20260814T131235Z-joongna-ram-category-pagination`
  - `/var/backups/used-market-runner/20260814T132110Z-shadow-pagination-stability`
- AWS 러너·터널은 `active`, 공개·로컬 health는 HTTP 200, 배포 파일 해시는 일치하며 운영 모드는 계속 `shadow`다.
- 주요 카테고리 첫 검증에서 패션·가전의 4페이지 수집이 Cloudflare의 기존 15초 검색 프록시 제한을 넘겨 D1 대체로 바뀌는 병목을 확인했다. 사용자가 느려도 품질을 우선한다는 운영 방향에 맞춰 `SEARCH_RUNNER_TIMEOUT_MS=60000`을 배포했고 설정 회귀 검사를 추가했다.
- 60초 제한 배포 뒤 중고나라 카테고리 실검색은 모두 AWS `live_compare`로 확인됐다. 패션 159개·6페이지, 가전 157개·6페이지, 뷰티 156개·6페이지, 가구 52개·2페이지, 게임 34개·2페이지, 모바일 130개·5페이지, PC 146개·5페이지, 오토바이(`category=19`) 159개·6페이지다.
- 최종 Cloudflare Worker 버전은 `19ecc971-fbbc-4cb2-97d5-e4c02651b6f6`이며 양 공개 도메인의 health와 32개 카테고리 응답이 통과했다.
- 최종 운영 상태는 DB 약 70.8MB, 활성 상품 4,462개, RSS 약 133MB, 여유 디스크 약 54.0GB, 대기·갱신 지연 0이며 DB·디스크·메모리·갱신 지연 경고가 모두 false다.
- 이 변경은 중고나라 수집 창과 키워드 판정을 바꿔 비교 누락률에 직접 영향을 준다. 전환 표본 기준을 `comparison_runs.created_at >= 2026-08-14T13:21:11.000Z`로 다시 잡았다. 프록시 설정까지 공개 반영된 시각을 포함해 가장 이른 전환 판단은 2026-08-15 22:35 KST로 연기한다.

## 2026-08-14 23:39~2026-08-15 00:04 KST 경쟁 서비스 비교·단계 확장 배포

- 경쟁 서비스 중고닷을 브라우저와 배포 JavaScript로 직접 확인했다. `RAM` 검색은 총 4,524개로 표시됐고 DOM 상품 링크는 처음 20개, 스크롤 뒤 40개로 늘었다. 그러나 스크롤마다 원 사이트를 다시 검색한 것이 아니라 처음 플랫폼별 `maxItems` 결과를 합친 뒤 클라이언트 배열을 `slice(0, 표시개수)`로 20개씩 더 렌더링하는 구조였다.
- 경쟁 서비스 첫 화면에는 `램프`, `슬램덩크`, `milgram` 등 RAM 메모리와 관계없는 결과가 섞였다. 표시 수량만 그대로 모방하지 않고 브라우저 전송량과 검색 품질을 함께 지키는 서버 단계 확장 방식을 채택했다.
- 우리 번호 페이지는 누를 때마다 `POST /api/search`로 다음 30개를 요청한다. 같은 서버 스냅샷 커서를 사용하므로 번호 페이지 이동만으로 원 사이트 수집은 실행하지 않는다.
- 저장 결과의 마지막 페이지에만 `다음 매물 더 찾기`를 표시한다. 이 버튼은 사이트별 수집 창을 160→320→480→640개로 한 단계 늘리고 검증·중복 제거 뒤 새 스냅샷을 만든다. 확장 창은 검색어에 저장하며 전체 스냅샷은 최대 1,000개다.
- 원본 페이지 상한은 단계 확장에 맞춰 번개장터·헬로마켓 최대 32페이지, 중고나라 최대 13페이지로 늘렸다. 브라우저는 전체 목록을 미리 받지 않고 계속 30개씩 받는다.
- 공개 요청 계수 검증에서 `DDR4 RAM` 첫 요청은 30개·원본 수집 1회였고 다음 30개 페이지는 중복 0개, `live_collection_runs_total` 증가 0, `source_collection_attempts_total` 증가 0이었다. 첫 수집 창의 검증 결과는 141개였다.
- 공개 UI의 중고나라 `RAM + 노트북/PC`는 73개·3페이지에서 시작했다. 마지막 페이지의 확장 버튼을 누르자 `새 매물 51개를 더 찾았습니다`가 표시되고 124개·5페이지로 늘어났다.
- 확장 목록에서 오토바이용 `RAM Mounts/램 마운트` 오탐 1건을 발견해 RAM 메모리 증거가 없는 마운트 상품을 제외했다. 최종 공개 API는 320 창에서 123개를 5페이지로 반환했고 고유 123개, 중복 0개, 해당 오탐 0개였다.
- 첫 창·확장 수집 실측은 원본 변동에 따라 160 창 66~73개, 320 창 123~125개, 480 창 176개였다. 개수를 강제로 채우지 않고 품질 필터를 통과한 수량만 사용한다.
- 전체 `npm test`가 통과했다. UI 페이지 계약 36개, 색인 계약 85개, 실시간 검색 계약 139개를 포함하며 Cloudflare dry-run과 양 공개 도메인의 health·32개 카테고리도 통과했다.
- AWS 최종 백업은 `/var/backups/used-market-runner/20260814T150047Z-progressive-pagination-quality`다. 배포 파일 해시가 일치하고 러너·터널은 `active`다. 최종 Cloudflare Worker 버전은 `f9c11d7b-5810-4963-9cf2-e44d22ddce84`다.
- 최종 운영 상태는 `shadow`, DB 81,756,160 bytes, 활성 상품 5,716개, 검색어 60개, 스냅샷 227개·29,838행, RSS 167,657,472 bytes, 여유 디스크 53,725,368,320 bytes, 대기 갱신 0개다. DB·디스크·메모리 경고는 없지만 갱신 지연 4,802초·2배 초과 12개로 `refresh_lag_warning=true`이므로 `cache_first`로 전환하지 않았다.
- 단계 확장과 RAM 오탐 교정이 비교 품질과 수집 창을 바꿨으므로 최종 전환 표본 기준은 `comparison_runs.created_at >= 2026-08-14T15:00:53.746Z`로 다시 잡았다. 가장 이른 전환 판단은 2026-08-16 00:10 KST이며 24시간·30회·5개 사이트·주요 카테고리·누락률 5% 이하·오래된 상품률 10% 이하·운영 경고 없음이 모두 필요하다.

## 2026-08-17 18:13~18:22 KST 자동 전환 검증

- 최종 공개 배포 후 24시간 조건은 지났지만 `comparison_runs.created_at >= 2026-08-14T15:00:53.746Z` 직접 집계의 최초 상태는 비교 7회·검색어 4개, 누락률 19.50%, 오래된 상품률 47.53%로 전환 기준에 미달했다.
- 직전 분산 표본 이후 3시간 이상 지난 것을 확인하고 한 실행의 허용 상한인 6회만 보강했다. `전체/아이폰15`, `가전/다이슨 에어랩`, `패션의류/나이키 후드티`, `게임/닌텐도 스위치`, `모바일/갤럭시 S24`, `PC/맥북 M2`가 모두 HTTP 200·`live_compare`·30개 응답이었고 5개 원본 사이트를 포함했다.
- 보강 후 직접 집계는 비교 12회·검색어 9개, 누락률 39.98%(1,042/2,606), 오래된 상품률 44.66%(1,402/3,139)였다. 5개 사이트와 전체·가전·패션의류·게임·모바일·PC 범주는 모두 포함됐지만 비교 30회, 누락률 5% 이하, 오래된 상품률 10% 이하를 충족하지 못했다.
- 공개 health와 인증된 색인 상태는 HTTP 200, AWS 러너·터널은 `active`, 최근 서비스 오류는 0, SQLite 무결성은 `ok`였다. DB는 142,561,280 bytes, 활성 상품 8,273개, RSS 193,167,360 bytes, 여유 디스크 52,146,970,624 bytes로 DB·디스크·메모리 경고는 없었다.
- 갱신 지연은 243,273초, 2배 초과 검색어는 20개로 `refresh_lag_warning=true`였다. 백그라운드 실행은 시간당 한도 12회를 모두 사용했지만 유효 검색어 갱신을 따라잡지 못했다.
- 원인은 레거시 `query_index` 7행의 `sites_json='[]'`였다. 최근 갱신 작업 120개 중 80개가 빈 사이트 요청으로 만들어져 `at least one target site is required`로 실패했고, 최근 72시간 서비스 로그에는 같은 실패가 585회 있었다. 특히 오래된 `설화수` 빈 사이트 검색어가 매분 우선 선택돼 시간당 12회 예산을 소진하면서 정상 검색어 갱신을 굶겼다.
- 다음 전환 판단 전 `dueQueries()`에서 빈 사이트 검색어를 격리하고 빈 사이트 등록을 막은 뒤, 레거시 7행을 안전하게 정리해야 한다. 이번 자동 검증은 진단·기록 범위이므로 운영 코드는 변경하지 않았다.
- 중고나라 PC `RAM` 스냅샷은 수집 창 480, 고유 상품 537개였고 페이지 중복과 `램스킨`, `램프`, `램 레더`, `RAM Mounts/램 마운트`, `슬램덩크`, `글램팜` 오탐은 0개였다. `아이폰15` 낮은 가격순 상위 10개에도 교환·매입·박스만·본체 제외·광고 노이즈가 없었다.
- 전체 `npm test`가 통과했고 로컬·AWS 러너/색인/실시간 검색 파일 해시는 일치했다. 단계 확장·번호 페이지·원본 페이지 상한·거짓 `refreshed_at` 방지 계약도 회귀 검사에 포함돼 있다.
- 전환 기준이 충족되지 않아 `RUNNER_INDEX_MODE=shadow`를 유지했다. `cache_first` 전환, 재시작, 전환 후 refresh 토큰·커서 재시작 연속성·D1 장애 대체 검증은 실행하지 않았고 자동 검증은 계속 유지한다.
